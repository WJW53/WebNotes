//只写一个大括号会理解为函数体,函数体里不能这么写a: a,
//改为{
    return {
        a: a,
    }
//}
//或者 ({a: a,})      因为这样的话,就会理解为表达式,返回这个表达式是可以的

const sum = (a, b) => ({
    a: a,
    b: b,
    sum: a + b
});

console.log(sum(3, 5))