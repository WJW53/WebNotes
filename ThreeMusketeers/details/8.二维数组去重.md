# 二维数组去重

```js
var trim = function(str){//去掉字符串首尾空格
    return str.replace(/(^\s*)|(\s*$)/g,"");
}


var duplicate =function(arr){
        // 法一：es6    ,法一是错的
        // let res=new Map();
        // arr.forEach(item=>{
        //     item.sort((a,b)=>a-b);
        //     res.set(item.join(),item);
        // });        
        // return Array.from(res.values);

        // 法二：   法二可行
        let res={}
        arr.forEach(item=>{
            item.sort((a,b)=>a-b);
            res[item]=item;
        });
        return Object.values(res)
    }

```